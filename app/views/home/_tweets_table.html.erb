<table class="table">
  <thead>
    <tr>
      <th>Tweets</th>
    </tr>
  </thead>
  <tbody>
  	<% tweets.each do |tweet| %>
    <tr>
    	<% if tweet[:sentiment] == 'Positive' %>
      	<td><span class="label label-success">Positive</span> <b><%= tweet[:date].strftime("%d/%m/%Y at %I:%M%p") %></b> - <%= tweet[:text] %></td>
      <% else %>
      	<td><span class="label label-danger">Negative</span> <b><%= tweet[:date].strftime("%d/%m/%Y at %I:%M%p") %></b> - <%= tweet[:text] %></td>
      <% end %>	
    </tr>
    <% end %>
  </tbody>
</table>